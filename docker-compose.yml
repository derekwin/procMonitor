services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:/app/data/proc.db
      - ADMIN_SECRET=change-this-in-production
      - CRON_SECRET=your-cron-secret-here
    volumes:
      - ./data:/app/data
    restart: unless-stopped