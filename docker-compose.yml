version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=/app/data/proc.db
      - ADMIN_SECRET=change-this-in-production
    volumes:
      - ./data:/app/data
    restart: unless-stopped